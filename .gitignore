# Local Claude settings (machine-specific permissions)
.claude/

# macOS
.DS_Store

# Secrets and machine-specific config
.env
.mcp.json
user_config.json
credentials/

# Package manager cache
.pnpm-store/

# Runtime state — personal data, updated every session, never push
portfolio_state.json
paper_trades.json
daily_signal.json
rebalancing_report.json
tax_report.json

# DuckDB database files — binary, not version controlled
*.duckdb
*.db
investment-os/data/
investment-os/backend/data/kite_token.json
